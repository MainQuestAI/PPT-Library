# Contract Drafts

本目录包含 v1.5～v2.0 规划中的 JSON Schema 草案。

规则：

1. 实施前必须与真实代码和 canonical cross-repo contract 核验。
2. `deck-master-selection.v1.schema.json` 是兼容草案；Canonical 仍应以 Deck Master 仓库为准。
3. Contract 变更必须更新 schema、example、tests、CHANGELOG。
4. Schema 与数据库 Schema 独立版本化。
5. examples 中只允许合成数据。
